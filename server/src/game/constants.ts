
const GRAVITY_POWER = 0.08;
const TURN_TIME = 30;
const WARMUP_TIME = 5;
const MAX_BULLET_START_SPEED = 3;
// rad = deg * pi / 180
const PITCH_RANGE = [(-30*Math.PI) / 180, (10*Math.PI) / 180];

export {
    GRAVITY_POWER,
    TURN_TIME,
    WARMUP_TIME,
    PITCH_RANGE,
    MAX_BULLET_START_SPEED,
}