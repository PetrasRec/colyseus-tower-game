
const GRAVITY_POWER = 5.0;
const TURN_TIME = 30;
const WARMUP_TIME = 5;

// rad = deg * pi / 180
const PITCH_RANGE = [(-30*Math.PI) / 180, (10*Math.PI) / 180];

export {
    GRAVITY_POWER,
    TURN_TIME,
    WARMUP_TIME,
    PITCH_RANGE,
}